# AROMI — Hugging Face Spaces requirements
# Lighter than full requirements.txt — torch/whisper omitted for faster cold start
# Voice transcription gracefully degrades to demo mode without faster-whisper

fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
httpx==0.27.0
python-multipart==0.0.9
alembic==1.13.1
reportlab==4.1.0
numpy==1.26.4
python-dotenv==1.0.1
aiofiles==23.2.1
Pillow>=10.4.0
twilio==9.3.2
# ChromaDB is optional — RAG falls back to embedded KB if not available
# chromadb==0.5.0
# sentence-transformers==3.0.0
